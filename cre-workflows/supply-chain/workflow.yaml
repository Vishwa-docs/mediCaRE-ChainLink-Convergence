staging-settings:
  user-workflow:
    workflow-name: "medicare-supply-chain-staging"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "medicare-supply-chain-production"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"
