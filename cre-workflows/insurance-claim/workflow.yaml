staging-settings:
  user-workflow:
    workflow-name: "medicare-insurance-claim-staging"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "medicare-insurance-claim-production"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"
