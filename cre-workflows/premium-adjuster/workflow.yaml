staging-settings:
  user-workflow:
    workflow-name: "medicare-premium-adjuster-staging"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "medicare-premium-adjuster-production"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"
