staging-settings:
  user-workflow:
    workflow-name: "medicare-auth-session-staging"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "medicare-auth-session-production"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"
