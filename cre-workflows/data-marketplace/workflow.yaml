staging-settings:
  user-workflow:
    workflow-name: "medicare-data-marketplace-staging"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "medicare-data-marketplace-production"
    workflow-artifacts:
      workflow-path: "."
      config-path: "./config.staging.json"
      secrets-path: "../../secrets.yaml"
